SBox Circuit

Программа принимает на вход число n - размерность, а затем подстановку из 2^n чисел от 0 до 2^n - 1. Каждое число интерпретируется как вектрор из 0 и 1.
По данным параметрам программа строит бинарное дерево по следующему правилу, выполняемому для каждого узла, начиная с корня:
 - находится пара векторов с наибольшим количеством общих единиц
 - вектор, состоящий только из общих единиц (и остальных нулей) кладется в правого потомка
 - два вектора разницы кладутся в левого потомка
 - если нет векторов с общими единицами или остался всего один вектор, процесс прекращается.
 
Далее программа считает сложность схемы из функциональных элементов в базисе конъюнкция, дизъюнкция, отрицание (не учитывается при учете сложности), которая реализует S-блок, заданный на входе. При вычислении сложности 
не учитывается сложность дешифратора размерности n.